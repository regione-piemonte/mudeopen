// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  isProduction: false,
  basePath: 'https://<VIRTUAL_HOST_DI_PUBBLICAZIONE_AMBIENTE_DEV>/mudeopen/rest',
  logoutUrl: 'https://<VIRTUAL_HOST_DI_PUBBLICAZIONE_AMBIENTE_DEV>/mudeopen-spids_liv2_sispliv2spid_gasp_regione/logout.do',
  frontoffice: 'https://<VIRTUAL_HOST_DI_PUBBLICAZIONE_AMBIENTE_DEV>/mudeopen/#/',

  // this enables in dev the template preview, not to be released in test environmnet yet
  
};